import { TableDragDirective } from './table-drag.directive';

describe('TableDragDirective', () => {
  it('should create an instance', () => {
    const directive = new TableDragDirective();
    expect(directive).toBeTruthy();
  });
});
