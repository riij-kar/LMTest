import { UserDetailDirective } from './user-detail.directive';

describe('UserDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new UserDetailDirective();
    expect(directive).toBeTruthy();
  });
});
